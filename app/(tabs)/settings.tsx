import { ComingSoon } from '@/components/ui/ComingSoon';

export default function SettingsTab() {
  return (
    <ComingSoon
      label="Settings"
      description="Budget configuration, reminders, biometric lock, and data export."
      icon="settings-outline"
    />
  );
}
